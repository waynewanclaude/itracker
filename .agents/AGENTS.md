# Workspace Rules

- **Strict Folder Protections**: Never modify the `user_resources/` folder or any of its contents.
- **Prime Directive**: Adhere strictly to the safety guidelines in [prime_directive.md](file:///c:/Projects/gemini/itracker/planning_and_spec/prime_directive.md) (never delete, rename, or destroy user-created folders or parent synchronization roots).
- **Communication & Alignment Directive**: Always understand the problem completely and make sure the user and the agent are on the same page (aligned on goals and assumptions) before taking action, proposing code changes, or performing bug fixes. Do not make assumptions or immediate edits without first listening and confirming understanding.
- **Code Presentation Limitation**: Never display a code block longer than 5 lines in the chat conversation.
- **Editor Navigation Requirement**: If code must be shown or referenced, write it/view it directly in the editor workspace panel, and provide a clickable `file://` link mapping to the exact code file and line numbers so the user can click and jump to it.
